# `@ubiquity-os/daemon-pull-review`

This is a plugin that wraps a Clade Code action with a custom prompt that runs on Claude Opus 4 with additional tools for better context.

## Installation

`.ubiquity-os.config.yml`:

```yml
plugins:
  - uses:
      - plugin: ubiquity-os-marketplace/daemon-pull-review
```
